process.container = 'ghcr.io/ebispot/monarch_pheval:dev'
process.containerOptions = '--user $(id -u):$(id -g)'

docker.enabled = true
docker.autoMounts = true
docker.runOptions = "-v ${System.getenv('PHEVAL_HOME')}:${System.getenv('PHEVAL_HOME')}"
